# Photo Bot Effects Configuration
# ═══════════════════════════════════════════════════════════════════════════════
#
# CATEGORIES:
#   enabled  - true/false to show/hide
#   order    - display order (lower = first)
#   label    - button text with emoji
#   parent   - parent category id (for subcategories)
#
# EFFECTS:
#   enabled  - true/false to show/hide (keeps data for analytics)
#   order    - display order within category (lower = first)
#   label    - button text with emoji
#   tips     - photo recommendations (optional)
#   category - category id (omit for top-level effects)
#
# Auto-resolved files (by effect_id):
#   prompts/{effect_id}.txt  — prompt text
#   images/{effect_id}.jpg   — example image
#
# To disable: set enabled: false (don't delete - keeps stats history)
# To reorder: change order values (sorted ascending)
#
# HIERARCHY EXAMPLES:
#   No category  → shows on "Create" screen (level 0)
#   category: X  → shows under category X (level 1)
#   parent: X    → makes a category a subcategory of X (level 2)
# ═══════════════════════════════════════════════════════════════════════════════

# ── Categories ────────────────────────────────────────────────────────────────

categories:
  trend:
    enabled: false
    order: 10
    label: "🔥 Тренды"

  style:
    enabled: true
    order: 1
    label: "👗 Стиль и мода"

  movie:
    enabled: false
    order: 2
    label: "🎬 Меняем стиль"

# ── Effects ───────────────────────────────────────────────────────────────────

effects:
  love_is:
    enabled: false
    order: 1
    label: "💌 Открытка в стиле Love is"
    tips: |
      📸 Рекомендации:
      • Лицо крупным планом
      • Хорошее освещение
      • Без очков
    category: trend

  cat_phone:
    enabled: true
    order: 2
    label: "🐱 Котик вместо телефона"
    tips: |
      📸 Рекомендации:
      • Держите телефон в руках
      • Руки должны быть видны
    category: trend

  afro:
    enabled: true
    order: 1
    label: "🧪 Тест промпта"
    category: style

  mullet:
    enabled: true
    order: 2
    label: "Маллет и усы"
    category: style

# ── TEMPLATE ──────────────────────────────────────────────────────────────────
#
# new_effect:
#   enabled: true
#   order: 10
#   label: "🎨 Display Name"
#   tips: |
#     📸 Рекомендации:
#     • Tip 1
#   category: style        # omit for top-level
#
# Then create: prompts/new_effect.txt
# Optionally:  images/new_effect.jpg
